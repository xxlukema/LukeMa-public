
package:prod

    taskList = ['clean:all', 'jshint', 'browserify:bundle', 'ngconstant:prod', 'ngtemplates', 'concat:js', 'compass', 'copy', 'uglify:dist', 'assetFingerprint', 'compress:prod', 'shell:resetFingerprints'];

watch:dev

    files: ['app/**', 'config/**', 'img/**', 'styles/**/*.scss', 'Gruntfile.js', 'gsr.html'],
    tasks: ['clean:all', 'newer:browserify:bundle', 'newer:jshint:app', 'ngconstant:dev', 'newer:ngtemplates', 'newer:concat:js', 'compass', 'newer:copy']

========================

serve:dev

    taskList = ['connect:server', 'watch:dev'];

watch/package starts:

1. clean.all.src: ['tmp', 'dist/*.js', 'dist/*.json', 'styles/app-*.css' , 'app/common.config.js']

2. browserfy:boundle

   browserify.bundle:
                src: 'app/app.js',
                dest: 'tmp/app.js'

3. ngconstant

   ngconstant.options:
                name: 'config',
                dest: 'app/common/config.js'

4. ngtemplates

   ngtemplates.app:
                src: 'app/**/*.html',
                dest: 'tmp/templates.js'

5. concat

   concat.js:
                src: ['tmp/*.js',
					'app/**/*.js',
					'!app/app.js',
					'node_modules/angular-material/angular-material.js',
					'node_modules/angular-ui-router/release/ng1/stateEvents.min.js',
					'node_modules/angular-animate/angular-animate.js',
					'node_modules/angular-aria/angular-aria.js',
					'node_modules/ng-file-upload/dist/ng-file-upload.min.js',
					'node_modules/angular-paging/dist/paging.min.js'],
                dest: 'dist/app.js'

6. compass

   compass.dist:
                options:
                    sassDir: 'styles/sass',
                    cssDir: 'styles'

7. copy

   copy.css:
                expand: true,
                flatten: true,
                filter: 'isFile',
                src: 'node_modules/angular-material/angular-material.css',
                dest: 'styles/'

<!-- watch ends here. -->

8. uglify

   uglify.dist:
                files:
                    'dist/app.js': ['dist/app.js']

8. assetFingerprint

9. compress:prod

10. shell:resetFingerprints'




