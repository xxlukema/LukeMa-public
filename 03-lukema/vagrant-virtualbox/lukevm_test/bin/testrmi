
cd $PROD_HOME

if [ ! -f client.properties ]
then
   cp ../test.properties/client.properties .
   cp ../test.properties/oasys.properties .
fi

CLASSPATH=.:../test:$DEV_HOME/build:../lib/lib.oasys/OasysRMI.jar:$WL_HOME/classes:$WL_HOME/lib/weblogicaux.jar

java sbc.webcct.beans.session.stateless.oasys.TestOasysRmi
