
/**
 * This test showsi:
 *    const testObj = {one: one, Two: Two};
 * can be simplified as:
 *    const testObj = {one, Two};
 *
 */
const one = 'One Line';
const Two = '222';
const testObj = { one, Two };
console.log('testObj', testObj);


