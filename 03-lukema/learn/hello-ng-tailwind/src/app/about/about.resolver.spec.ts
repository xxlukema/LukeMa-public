

xdescribe('aboutResolver', () => {
  /**
  const executeResolver = (route: any, state: any): Observable<UserResponse> =>
      TestBed.runInInjectionContext(() => new UserResolver(TestBed.inject<UserService>(new UserService())).resolve(route, state));

  beforeEach(() => {
    TestBed.configureTestingModule({});
  });

  it('should be created', () => {
    // Provide mock route and state as needed for your resolver
    const mockRoute = {} as any;
    const mockState = {} as any;
    expect(executeResolver(mockRoute, mockState)).toBeTruthy();
  });
  */
});
