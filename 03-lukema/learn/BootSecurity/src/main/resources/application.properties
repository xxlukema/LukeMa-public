
# logging.level.org.springframework = DEBUG

# sring.security.filter-dispatcher-types=ASYNC, FORWARD, INCLUDE, REQUEST, ERROR

# spring.jpa.properties.hibernate.current_session_context_class=org.springframework.orm.hibernate5.SpringSessionContext

## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
# spring.datasource.url = jdbc:mysql://localhost:3306/notes_app?useSSL=false
# spring.datasource.url = jdbc:postgresql://localhost:5432/test?currentSchema=luke
spring.datasource.url = jdbc:postgresql://localhost:5432/test
spring.jpa.properties.hibernate.default_schema=luke
spring.datasource.username = luke
spring.datasource.password = ENC(P3Tt8KdEzfU5+YWwHatbvA==)
# JASYPT_ENCRYPTOR_PASSWORD=luke-key
jasypt.encryptor.password=${JASYPT_ENCRYPTOR_PASSWORD:}
# JASYPT_ENCRYPTOR_ALGORITHM=PBEWithMD5AndDES
jasypt.encryptor.algorithm=${JASYPT_ENCRYPTOR_ALGORITHM:}


## Hibernate Properties
spring.data.jpa.repositories.enabled = true
spring.jpa.generate-ddl = false
spring.jpa.hibernate.ddl-auto = none
spring.jpa.show-sql = true

spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults = false

# The SQL dialect makes Hibernate generate better SQL for the chosen database
# spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
# spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect

# Hibernate ddl auto (create, create-drop, validate, update)
# spring.jpa.properties.hibernate.ddl-auto = create
# spring.jpa.properties.hibernate.show-sql=false
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.properties.hibernate.use_sql_comments=true

# spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

spring.jta.enabled = true

###############################################

# spring.application.index= index.html
# spring.application.name= HelloHoustonService

# spring.config.location= # Config file locations.
# spring.config.name=application # Config file name.

spring.mvc.date-format= dd/MM/yyyy

# spring.webservices.path=/spring

spring.batch.job.enabled = false

server.port: 8443
# JKS_DIR = C:/JavaCAPS/appserver/domains/boot/config
server.ssl.key-store: ${JKS_DIR:}/lukekeystore.jks
server.ssl.key-store-password: lukema
server.ssl.keyStoreType: PKCS12
server.ssl.keyAlias: luke


