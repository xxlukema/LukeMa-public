import { MinusSignToParensPipe } from './minus-sign-to-parens.pipe';

describe('MinusSignToParensPipe', () => {
    it('create an instance', () => {
        const pipe = new MinusSignToParensPipe();
        expect(pipe).toBeTruthy();
    });
});
