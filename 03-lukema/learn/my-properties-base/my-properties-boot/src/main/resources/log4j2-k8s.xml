<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE xml>
<Configuration status="WARN" monitorInterval="30" strict="true" includeLocation="true">
    <Properties>
        <Property name="logPath">/home/lma/logs</Property>
        <Property name="rollingFileName">my-properties-boot</Property>
    </Properties>
    <Appenders>
        <Console name="CONSOLE" target="SYSTEM_OUT">
            <PatternLayout pattern="#### %d{yyyy-MM-dd HH:mm:ss} [%-5p] %c(%L) %M() %n%m%n" />
        </Console>
        <Async name="ASYNC">
            <AppenderRef ref="CONSOLE" />
        </Async>
        <RollingFile name="rollingFile" fileName="${logPath}/${rollingFileName}.log" filePattern="${logPath}/${rollingFileName}_%d{yyyy-MM-dd}.log">
            <PatternLayout pattern="#### %d{yyyy-MM-dd HH:mm:ss} [%-5p] %c(%L) %M() %n%m%n" />
            <Policies>
                <!-- Causes a rollover if the log file is older than the current JVM's start time -->
                <OnStartupTriggeringPolicy />
                <!-- Causes a rollover once the date/time pattern no longer applies to the active file -->
                <TimeBasedTriggeringPolicy interval="1" modulate="true" />
            </Policies>
        </RollingFile>
    </Appenders>

    <Loggers>
        <Logger name="com" level="debug" additivity="false">
            <AppenderRef ref="rollingFile" />
        </Logger>

        <Root level="error">
            <AppenderRef ref="rollingFile" />
            <AppenderRef ref="ASYNC" />
        </Root>
    </Loggers>
</Configuration>
