# flexbox

- If you want to use Flexbox in your React project, you actually don't need to install any special extension or package.
- Flexbox is just a native part of CSS, and React supports it out of the box.
