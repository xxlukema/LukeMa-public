# Route

    npm install react-router-dom

## How to edit Router

1. Create a new page in `src/components/my-page` directory.
2. Edit `src/main.tsx` to add/remove a routed page.
3. Edit `src/components/layout/Layout.tsx`.
