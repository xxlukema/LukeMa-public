<?xml version="1.0" encoding="UTF-8"?>
<project name="build_jboss" default="deploy_web" basedir=".">
   <import file="./build_maven.xml" />

   <property environment="env" />

   <property name="JBOSS_APP" value="${env.JBOSS_HOME}/server/apptricityerm50" />

   <property name="JBOSS_APP_TMP" value="${JBOSS_APP}/tmp" />

   <property name="JBOSS_APP_LOCALHOST" value="${JBOSS_APP}/work/jboss.web/localhost" />

   <property name="JBOSS_APP_LOG" value="${JBOSS_APP}/logs" />

   <target name="deploy_web" depends="clean_jboss">
      <deploy_web_macrodef dir="." />
   </target>

   <target name="clean_jboss">
      <delete verbose="false" includeemptydirs="true" quiet="true">
         <fileset refid="tmp">
         </fileset>

         <fileset refid="log">
         </fileset>

         <fileset refid="localhost">
         </fileset>
      </delete>
   </target>

   <fileset id="tmp" dir="${JBOSS_APP_TMP}">
      <include name="**/*" />
   </fileset>

   <fileset id="log" dir="${JBOSS_APP_LOG}">
      <include name="**/*" />
   </fileset>

   <fileset id="localhost" dir="${JBOSS_APP_LOCALHOST}">
<!-- Exclude original dirs from delete -->
      <exclude name="_" />

      <exclude name="admin-console" />

      <exclude name="invoker" />

      <exclude name="jbossws" />

      <exclude name="jmx-console" />

      <exclude name="web-console" />
   </fileset>
</project>

