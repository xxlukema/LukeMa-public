
spring:
    application:
      name: HelloHoustonService

    datasource:
      url: jdbc:postgresql://localhost:5432/test
      username: luke
      password: luke

    jpa:
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
      hibernate:
        ddl-auto: none

---
