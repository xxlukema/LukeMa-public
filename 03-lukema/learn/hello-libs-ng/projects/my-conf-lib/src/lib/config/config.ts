import { InjectionToken } from '@angular/core';

export const APP_CONFIG = new InjectionToken<any>('Application config');
export const MY_CONFIG = new InjectionToken<any>('Application my config');
