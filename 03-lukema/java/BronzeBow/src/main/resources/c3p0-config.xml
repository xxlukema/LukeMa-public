<?xml version="1.0" encoding="UTF-8"?>
<c3p0-config>
	<default-config>
		<property name="maxPoolSize">10</property>
		<property name="minPoolSize">3</property>
		<property name="initialPoolSize">5</property>
		<!-- maxIdleTime
	    	 Seconds a Connection can remain pooled but unused 
		     before being discarded. Zero means idle connections never expire.
		 -->
		<property name="maxIdleTime">1800</property> <!-- 30 minutes -->
		<property name="maxStatementsPerConnection">5</property>
		<!-- checkoutTimeout
		     The number of milliseconds a client calling getConnection() will 
		     wait for a Connection to be checked-in or acquired when the pool is 
		     exhausted. Zero means wait indefinitely.
		 -->
		<property name="checkoutTimeout">120000</property> <!-- 2 minutes -->
		<extensions>
			<property name="initSql">select now()</property>
		</extensions>
		<property name="preferredTestQuery">select now()</property>
	    <property name="testConnectionOnCheckin">true</property>
	    <property name="testConnectionOnCheckout">false</property>
	    <!-- idleConnectionTestPeriod
	         If this is a number greater than 0, c3p0 will test all idle, 
	         pooled but unchecked-out connections, every this number of seconds.
	     -->
	    <property name="idleConnectionTestPeriod">600</property> <!-- 10 minutes -->
	</default-config>
</c3p0-config>
