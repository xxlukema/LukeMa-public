<project>
   <modelVersion>4.0.0</modelVersion>

   <groupId>com.learn</groupId>

   <artifactId>maven-parms-plugin</artifactId>

   <packaging>maven-plugin</packaging>

   <version>1.0</version>

   <dependencies>
      <dependency>
         <groupId>org.apache.maven</groupId>

         <artifactId>maven-project</artifactId>

         <version>2.1.0</version>
      </dependency>

      <dependency>
         <groupId>org.apache.maven</groupId>

         <artifactId>maven-plugin-api</artifactId>

         <version>2.1.0</version>
      </dependency>
   </dependencies>

   <build>
      <plugins>
         <plugin>
            <artifactId>maven-compiler-plugin</artifactId>

            <configuration>
               <source>1.5</source>

               <target>1.5</target>
            </configuration>
         </plugin>

         <plugin>
            <groupId>com.learn</groupId>

            <artifactId>maven-parms-plugin</artifactId>

            <configuration>
               <greeting>Welcome to ${artifactId}</greeting>

               <name>Name Name</name>
            </configuration>
         </plugin>
      </plugins>
   </build>
</project>

